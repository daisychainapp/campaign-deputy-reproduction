# Read-only key is enough for `ruby repro.rb <person_id>` (edit made in the web UI)
CAMPAIGN_DEPUTY_KEY=
# Key with write access, only needed for --api and --patch
FULL_PERMISSIONS_CAMPAIGN_DEPUTY_API_KEY=
